# TOOLS.md — Sentinel (intel)

## Permitido
- `web_fetch` — fontes fixas (business-monitor, edital, politics)
- `web_search` — DuckDuckGo / Tavily para pesquisas aprofundadas
- `mcp-trends` — leitura/escrita de trends no banco
- `read` — ler skills e referências
- `exec` — rodar scripts de skills

## Proibido
- `browser` — não necessário para monitoramento (exceto edital_monitor com JS-heavy)
- `mcp-crm` — não é domínio do intel
- `mcp-leads` — não é domínio do intel
- `canvas` — fora do escopo
- `obsidian` — vault é domínio do ops
