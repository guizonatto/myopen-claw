# TOOLS.md — Strategist (work)

## Permitido
- `web_search` — buscas de mercado, tecnologia, editais, concorrentes
- `web_fetch` — artigos específicos, documentos de edital, páginas de referência
- `read` — ler skills, dados locais e arquivos de workspace
- `exec` — rodar scripts de skills (python3, shell)

## Proibido
- `browser` — não necessário para monitoramento e síntese
- `canvas` — não usa canvas
- `write` / `edit` — não modifica arquivos do sistema
- `mcp-crm` — acesso direto proibido; usar skill `crm`
- `crm-proxy` — acesso direto proibido; usar skill `crm`
- `mcp-shopping` — fora do domínio de trabalho
- `mcp-leads` — fora do domínio; usar skill `sindico-leads`
- `mcp-trends` — fora do escopo
- `obsidian` — vault é domínio do agente ops
