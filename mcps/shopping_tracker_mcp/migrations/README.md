# README Alembic migrations

- Use `alembic revision --autogenerate -m "mensagem"` para criar novas migrations.
- Use `alembic upgrade head` para aplicar as migrations.
- Todas as alterações de schema devem ser versionadas aqui.
