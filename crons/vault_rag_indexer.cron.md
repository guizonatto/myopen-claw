openclaw cron add --name "vault-rag-indexer" --cron "0 3 * * *" --tz "${CRON_TZ:-America/Sao_Paulo}" --session isolated --no-deliver --message "Index the Obsidian knowledge vault. Use the obsidian MCP to list and read all .md files under /vault/2000-Knowledge. For each file, store or update its content as a semantic memory so it can be retrieved in future sessions."
