openclaw cron add --agent ops --name "vault-rag-indexer" --cron "0 3 * * *" --tz "${CRON_TZ:-America/Sao_Paulo}" --session isolated --no-deliver --message "Index the Obsidian knowledge vault using obsidian MCP kebab-case tools only. Steps: (1) call list-available-vaults and select vault 'vault' when available, otherwise use the first vault; (2) call search-vault with query=.md, path=2000-Knowledge, searchType=filename; (3) for each matched file path, split into folder and filename and call read-note; (4) for each note content, store or update semantic memory entries so the knowledge can be retrieved in future sessions."
