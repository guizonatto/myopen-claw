openclaw cron add --name "obsidian-git-push" --cron "*/5 * * * *" --tz "${CRON_TZ:-America/Sao_Paulo}" --session isolated --no-deliver --agent default --message "Check for uncommitted changes in the Obsidian vault by running: git -C /vault status --porcelain. If there are changes, run: git -C /vault add -A && git -C /vault commit -m \"vault: auto-sync\" && git -C /vault push origin main. If no changes, do nothing and stay silent."
