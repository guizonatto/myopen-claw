openclaw cron add --name "obsidian-git-push" --cron "*/5 * * * *" --tz "${CRON_TZ:-America/Sao_Paulo}" --session isolated --no-deliver --agent ops --message "Execute this git sync flow for /vault and report only errors: (1) run 'git -C /vault fetch origin main --quiet 2>&1'; (2) if 'git -C /vault status --porcelain' is not empty, run 'git -C /vault add -A && git -C /vault commit -m \"vault: auto-sync\"'; (3) if local is behind remote, run 'git -C /vault pull --rebase origin main'; (4) if local is ahead of remote, run 'git -C /vault push origin main'; (5) if there is nothing to commit and nothing to push, stay silent."
