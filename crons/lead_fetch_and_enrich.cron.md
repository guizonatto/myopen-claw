openclaw cron add --name "Lead Fetch and Enrich" --cron "*/30 * * * *" --tz "${CRON_TZ:-America/Sao_Paulo}" --session isolated --light-context --model "usage-router/ollama/gemma4:e2b" --thinking minimal --timeout-seconds 600 --message "Chame a tool MCP `sindico_leads` do servidor `mcp-leads` com params={max_results: 20}. Responda SOMENTE com o texto retornado pela tool (sem PII, sem JSON, sem explicações)." --announce --channel discord --to "channel:${DISCORD_CHANNEL_ID}"
