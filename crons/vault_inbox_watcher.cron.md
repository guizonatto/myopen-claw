openclaw cron add --name "vault-inbox-watcher" --cron "*/5 * * * *" --tz "${CRON_TZ:-America/Sao_Paulo}" --session isolated --no-deliver --agent librarian --message "List 4000-Inbox using the obsidian MCP. If no unprocessed .md files exist, output nothing and stop. If files exist: for each one, read its content, apply 3000-Agents/Librarian_SOP, then move it to the appropriate folder."
